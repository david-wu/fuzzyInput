import { TestBed, async } from '@angular/core/testing';
import { InnerHtmlLightComponent } from './inner-html-light.component';

describe('InnerHtmlLightComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        InnerHtmlLightComponent
      ],
    }).compileComponents();
  }));

});
